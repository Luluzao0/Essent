import React from 'react';
import SimpleVideoSection from '../components/SimpleVideoSection';

const Videos: React.FC = () => {
  return (
    <div style={{ paddingTop: 'clamp(4rem, 8vw, 6rem)' }}>
      <SimpleVideoSection />
    </div>
  );
};

export default Videos;
